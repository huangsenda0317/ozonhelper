# 本地覆盖配置（复制为 .env.local，git 忽略）
#
# 本地开发（推荐，配合 next.config.js 代理）：
NEXT_PUBLIC_API_BASE_URL=/api/v1
API_PROXY_TARGET=http://127.0.0.1:8000
#
# 若不想走 Next 代理、直连后端，可改为：
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
